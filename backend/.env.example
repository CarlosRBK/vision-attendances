# Backend environment example
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
DB_NAME=attendance_db

# Optional defaults for device
DEVICE_ID=default
DEVICE_TYPE=webcam
DEVICE_WIDTH=640
DEVICE_HEIGHT=480
DEVICE_FPS=30
DEBOUNCE_SECONDS=300

# App
APP_VERSION=0.1.0
